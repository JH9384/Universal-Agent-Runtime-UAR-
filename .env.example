# UAR Environment Configuration

# API Configuration
API_HOST=127.0.0.1
API_PORT=8000
API_WORKERS=1

# Environment
ENVIRONMENT=development
DEBUG=false

# Paths
PROJECT_ROOT=/path/to/project
UAR_LIBRARY_DIR=.uar_library

# Limits
UAR_MAX_UPLOAD_BYTES=52428800

# Security - IMPORTANT: Change this in production!
# Generate a new secret key with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-here-must-be-changed-in-production

# API Keys (format: key:user:tier,key2:user2:tier2)
# Example: dev-key-12345:developer:authenticated,prod-key-67890:production:authenticated
# IMPORTANT: Do not use default keys in production! Generate new keys.
API_KEYS=dev-key-change-in-production:developer:authenticated

# Logging
LOG_LEVEL=info

# Security & Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_ANONYMOUS=10
RATE_LIMIT_AUTHENTICATED=100
RATE_LIMIT_WINDOW=60

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# File Storage
RUNS_DIR=runs
MAX_FILE_SIZE=10485760  # 10MB in bytes
MAX_FILES=1000
MAX_REQUEST_SIZE=10485760  # 10MB

# Ollama integration (optional)
OLLAMA_HOST=http://127.0.0.1:11434
OLLAMA_MODEL=llama3.2:3b
OLLAMA_TIMEOUT_SECONDS=60

# Monitoring (optional)
ENABLE_METRICS=false
METRICS_PORT=9090

# Future:
# DATABASE_URL=
# REDIS_URL=

# Production Deployment Notes:
# 1. Always set a custom SECRET_KEY in production
# 2. Generate new API_KEYS - never use default/development keys
# 3. Set ENVIRONMENT=production
# 4. Set DEBUG=false
# 5. Configure proper CORS_ORIGINS for your domain
# 6. Consider using Redis for rate limiting in high-traffic scenarios
