# UAR Architecture

## Layers
- Object Layer (identity, storage)
- Runtime Layer (registered code)
- Execution Layer (bounded execution)
- Workflow Layer (composition)
- Conformance Layer (tests + invariants)

## Flow
object → runtime → execution → output → lineage
